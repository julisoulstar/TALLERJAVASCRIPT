    var height = prompt("write the measure of the height of the rectangle");
    var base = prompt("write the measure of the base of the rectangle");
    perimeter=(2*parseInt(height))+(2*parseInt(base));
	area=parseInt(height)*parseInt(base);
    console.log("the perimeter of the rectangle is : " + perimeter);
    console.log("the area of the rectangle is : " + area);